# A7 — Adversarial-Simplification-Critic — Round-1 Report (PRESERVED)

> **Provenance.** Final report preserved as delivered (cutoff 2026-07-21); substantive content faithfully retained, prose lightly condensed, nothing altered/added; complete raw = task-output JSONL. **SOURCE_REPORT_PRESERVED.** Context-isolated Claude subagent (same model family/provider). Round-1 conducted without access to other agents' findings.

---

**Stance & headline:** default hypothesis (thin rigorous edge-probe > full platform on time-to-evidence) **largely confirmed** — with a twist against a *naive* thin probe: the one place complexity earns its keep is the Release-0 truth kernel itself (PIT, purged/embargoed CV, survivorship-safe universe, append-only ledger). A quick-and-dirty probe that skips those produces a leakage-inflated false positive, worse than no probe. Correct simplification = "rigor concentrated on the go/no-go, everything else frozen as a someday-appendix." Prior direction (truth-kernel-first, defer advanced) CONFIRMED and already latent in the spec (REQ-REL4-003 "specification maximalism is a release blocker"; REQ-PROD-017/020; REQ-RELCLASS-007/008). Re-scope: dominant risk is NOT the architecture (unusually disciplined) but (a) documentation-as-progress theatre, (b) two subsystems over-specified even for their target releases, (c) the Counterfactual Factory mis-elevated to "product identity" (REQ-PROD-003), (d) under-specified go/no-go statistics and non-PIT data.

## PRE-MORTEM (ranked by probability×impact)
1. No edge / edge below cost floor (High/Fatal, Grade B). 2. Spec-maturity mistaken for implementation-maturity — 5,599-line spec + ~34 entities + zero tests + phantom code (High/High, Grade A repo fact). 3. Silent leakage/survivorship/PIT bug invalidates the go/no-go; single-path WF high path-variance (Med-High/Fatal, Grade A). 4. Scope gravity — never reaches the baseline report (Med-High/High, Grade B). 5. Throughput collapse/abandonment (Med/High, Grade C). 6. Threshold-freeze deadlock or post-hoc gaming (Med/Med-High, Grade A doc fact). 7. PIT data assumption false/expensive (Med/High, Grade B). 8. Rabbit-hole capabilities promoted early (Low-Med/Med, Grade C).

## SUBSYSTEM VERDICTS
Market World Model + hypotheses → **SIMPLIFY for R0/1A** (deterministic regime vector: SPY trend, VIX, breadth, rates; multi-hypothesis machinery DEFER to 2A+). Opportunity Memory → **DEFER; use family lineage** (`recommendation_family_id`+`parent_recommendation_id` already solve lineage). Knowledge Graph → **REJECT for R0–2A** (membership tables + rolling correlation suffice; full ontology/temporal edges have no consumer until a news expert exists). Evidence Dependency Graph → **REJECT for R1A (critical)** — R1A has exactly ONE expert (Technical); `independent_information_gain`/`dependency_edges` over one source are definitionally empty. Counterfactual Learning Factory → **DEFER hard (2B) — keep the cheap enabling subset** (append-only ledger + cohort logging from day one; replayable counterfactuals cheap, model-based ones deferred). LLM explanation → **SIMPLIFY now; LLM RESEARCH_ONLY** (deterministic templates auditable/reproducible/PIT-safe/no-hallucination; LLM in DECISION_RUNTIME violates REQ-PROD-012 determinism). Learned super-model → **RETAIN frozen policy; REJECT learned weighting R1A**. Feature store (service) → **SIMPLIFY — keep CONTRACT, reject SERVICE**. Event sourcing → **RETAIN ledger; do NOT event-source operational state** (spec's REQ-LEARN-004 pragmatic middle is right). **Where complexity earns its keep:** PIT correctness, purged+embargoed CV, survivorship-safe universe, deterministic replay, `feature_snapshot`, append-only ledger.

## FINDINGS
- **F-A7-01** Phantom implementation / documentation-as-progress. Grade A · blocks architecture_freeze, release_0_definition, release_0_acceptance. Reset matrix rows to DOCUMENTED/DESIGNED until code lands with a runnable test ID.
- **F-A7-02** Go/no-go judged on single-path walk-forward; no CPCV/DSR/PBO acceptance statistic. Grade A · blocks release_0_acceptance, final_holdout. Require CPCV (or CSCV) + Deflated Sharpe + PBO threshold, frozen before the run. *[Lead: applicability-conditioned in PHASE_1_EVIDENCE_INTEGRITY_REVIEW.md — not universal hard gates.]*
- **F-A7-03** PIT data-source assumption unbudgeted (retail feeds re-adjusted/revised/survivorship-affected). Grade B · blocks release_0_definition, release_0_acceptance. Name a survivorship-safe PIT universe source; golden test proving delisted names present and adjustments as-of-date.
- **F-A7-04** Evidence Dependency Graph inert in R1A (one expert). Grade A (logical) · blocks release_1A (as over-scope). Mark `evidence_dependency_graph` DEFERRED-2A; keep only flat `evidence_packet` in R1A.
- **F-A7-05** Counterfactual Factory mis-elevated to "product identity" (REQ-PROD-003) → scope gravity. Grade B · blocks release_0_definition (prioritization), paper. Re-brand decision+abstention+ledger as identity; keep only ledger+cohort logging early; defer twin/challengers to 2B.
- **F-A7-06** Readiness thresholds all TBD_BLOCKER → post-hoc gaming risk. Grade A · blocks final_holdout, shadow, paper. Freeze numeric thresholds before the holdout in a signed policy bundle.
- **F-A7-07** ~34-entity aggregate for a zero-test system; R1A needs ~10 (decision_cycle, recommendation, forecast_snapshot, forecast_distribution, decision_assessment, risk_assessment, outcome_record, feature_snapshot, decision_ledger_event, audit_event). Grade B · blocks release_1A. Implement the ~10-entity core; mark the other ~24 DESIGNED-only.

## TWO SIMPLER ARCHITECTURES + ESCALATION GATES
**ARCH-A "Rigorous Edge Probe"** (build first; ≈ R0 + sliver of 1A): survivorship-safe PIT universe → one canonical OHLCV adapter → 5/8/14 matured labels → CPCV (purged+embargo) → explicit transaction-cost model → one baseline (GBT over a small technical pack) → DSR+PBO report → append-only run ledger + feature_snapshot manifests. Regime as deterministic feature columns; no world-model entity, hypotheses, evidence-DAG, graph, opportunity entity, LLM, counterfactual twin. Escalation → ARCH-B: baseline beats naive comparator net-of-cost AND DSR significant at pre-frozen confidence AND PBO below threshold AND stable across CPCV paths AND ≥2 regimes. Fail any → `BLOCKED_NO_EDGE`, stop. **ARCH-B "Frozen-Policy Decision Kernel"** (only if A clears): 7-component kernel with frozen interpretable Alpha/Permission/Risk/Utility policy + NO_ACTION + calibration + portfolio-aware sizing; ~10-entity aggregate; deterministic templated explanations; append-only ledger with full cohort logging. Escalation → 2A context modules (each on demonstrated marginal value; evidence-DAG meaningful now with ≥2 experts) → 2B Learning Factory (after ≥N matured recommendations for counterfactual support). Both are strict subsets of the existing spec — an execution order and a freeze line, not a rewrite.

## HIDDEN ASSUMPTIONS
1. Exploitable net-of-cost edge exists (UNPROVEN). 2. Retail data is PIT-safe (LIKELY FALSE). 3. Spec maturity ⇒ implementation maturity (FALSE — zero code/tests, phantom files). 4. Single-path walk-forward is a sufficient gate (WEAK). 5. Multi-hypothesis world model + counterfactuals add value before edge proven (UNSUPPORTED pre-edge).

## VERDICT + TOP-5
Prior truth-kernel-first/defer-advanced direction CONFIRMED and latent in the spec. Danger is maximalist documentation treated as progress, two over-specified subsystems (evidence-DAG, world-model hypotheses), and the Counterfactual Factory mis-branding. Re-scope, don't rewrite: execute ARCH-A, freeze §3.7–3.10 and §23 as an un-built appendix, let evidence of net-of-cost edge (not documentation) unlock the platform. **Top-5:** (1) execute ARCH-A only; (2) fix the gate statistic (CPCV+DSR+PBO); (3) reset the coverage matrix; (4) delete over-scope from R1A (evidence-DAG/world-hypotheses/opportunity/KG/LLM; keep ledger+cohort logging); (5) freeze thresholds and name a PIT data source before the holdout.

## SOURCE ENTRIES (agent-reported; re-graded in SOURCE_EVIDENCE_REGISTER.md)
Bailey & López de Prado 2014 DSR [A]; Bailey et al. PBO/CSCV [A]; CPCV-vs-WF (Knowledge-Based Systems 2024; Wikipedia purged-CV) [B]; survivorship-bias magnitude (quantifiedstrategies; Elton-Gruber-Blake) [B]; short-horizon ML edge thin after costs (ScienceDirect; AFA viewpoint) [B/C]; Fowler MonolithFirst [B]; YAGNI/over-engineering [C].
