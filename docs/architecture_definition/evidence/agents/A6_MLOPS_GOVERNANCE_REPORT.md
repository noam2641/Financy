# A6 — Production-MLOps-&-Governance-Architect — Round-1 Report (PRESERVED)

> **Provenance.** Final report preserved as delivered (cutoff 2026-07-21); substantive content faithfully retained, prose lightly condensed, nothing altered/added; complete raw = task-output JSONL. **SOURCE_REPORT_PRESERVED.** Context-isolated Claude subagent (same model family/provider).

---

**Scope:** ops, reproducibility, governance, control-plane, MLOps. Repo confirmed docs-only (HEAD one commit past baseline), zero code/tests. **Anti-anchoring:** largely CONFIRMS the contradictory hypothesis (full control-plane/event-sourcing/registry stack over-scoped for R0/R1A; off-the-shelf + versioned hashed artifacts suffice pre-paper) — but several prior "blocker" framings are wrong in the other direction: modular monolith, small kernel, research/runtime split are genuinely proportionate and must NOT be complexified.

## FINDINGS
- **A6-F1** Coverage matrix cites code that does not exist in the repository (`Technical Analysis.py:6990`, `stock_ai_4_files/*`, `news_analysis.py`, `lstmfinancy.py`, `model.py`, `outputs/*`, `data/pipeline_checkpoints/*`). CONFIRMED (verified by `ls`) · HIGH · blocks release_0_acceptance, release_0_definition. The matrix declares itself the "operational source of truth for what is actually implemented" → corrupts the source of truth. Action: reset every code cell to `NONE_IN_REPO`/`DOCUMENTED`.
- **A6-F2** Reproducibility acceptance self-contradictory (bit-exact hashes vs "0 or approved tolerance") and bit-exact often physically infeasible. HIGH · R0-BL-002 vs REQ-GATE-001/REQ-LEARN · blocks architecture_freeze, release_0_acceptance. GPU/BLAS/cuDNN non-associative atomic accumulation + kernel-selection heuristics → not bit-identical without heavy determinism flags. Recommend **two-tier**: bit-exact for symbolic/config artifacts (fold manifests, feature-column registry, label frames, config, code SHA, schema hashes); seeded+numeric tolerance for float arrays with documented `allowed_tolerance` (field exists at REQ-TEST-004). R0 CPU-only/deterministic-mode. Grade A (contradiction) + B (nondeterminism).
- **A6-F3** `environment_hash` required (lines 469, 1083) but composition defined nowhere, and omitted from checkpoint-resume verification set (REQ-CONV-OVN-006). MED · blocks release_0_acceptance, release_1A. Recommend `environment_hash = H(python_version, key_library_versions_pinned, blas_impl+version, cuda/cudnn_or "cpu", container_image_digest, os_release)`; add to OVN-006 resume + runtime_manifest.
- **A6-F4** Schema-hash parity (`feature_schema_hash`) does not prevent train/serve value or freshness skew; but build-your-own versioned Parquet+hashes is the proportionate choice pre-paper. MED · blocks release_1A. External [B]: schema parity guarantees columns/types not freshness/value parity. Recommend a golden train/serve **value-parity** test; keep `feature_snapshot` as versioned Parquet + content hashes; **no** off-the-shelf online feature store pre-paper.
- **A6-F5** Full event-sourcing + hash-chained ledger + transactional outbox (REQ-LEARN-001/002, REQ-NFR-004) over-scoped for R0/R1A (most event types can't fire; REQ-LEARN-004 itself concedes "event-driven logically, modular-monolith physically"). MED (over-scope) · blocks nothing (a simplification). Recommend append-only audit ledger (Postgres, monotonic `sequence_number`, correlation/causation IDs) WITHOUT hash-chain/outbox for R0/R1A; defer hash-chain+outbox to R3 (keep field schema stable so later hardening is additive). Full chain still **required before shadow**.
- **A6-F6** Model/feature/policy/schema registries: buy, don't build, for a solo team (REQ-GOV-009/REQ-DOC-010). MED · blocks nothing (build-vs-buy). Recommend MLflow-style tracking+registry; policy_bundle/schema_version as versioned checksummed files in git + object storage + thin promotion table.
- **A6-F7** Runtime manifest / Control Plane as R1A RELEASE_BLOCKER (R1A-BL-006): manifest proportionate; full Control-Plane product premature. MED · blocks release_1A (only the minimal-manifest slice should). Keep minimal signed `runtime_manifest` (code/schema/provider versions, checksums, enabled/disabled/degraded capabilities) + capability-health as the R1A blocker (safety-relevant); defer full registries/promotion engine (kill_switches/environment_promotion/release_compatibility) to shadow-era.
- **A6-F8** Delayed-outcome monitors lag ~14 sessions; leading indicators specified but not designated as the early-warning gate (REQ-MON-001). LOW-MED · blocks shadow (bands frozen before shadow). Recommend designating immediate monitors (PSI on feature/prediction distributions, abstention-rate deviation, model-disagreement) as leading-drift gate; delayed monitors as confirmation. Keep, don't complexify.
- **A6-F9** Governance independence: a solo operator cannot self-satisfy independent validation or two-person kill-switch reset; this correctly caps solo operation at RESEARCH/SHADOW. HIGH (governance correctness) · REQ-GOV-001/002, REQ-KILL-001 · blocks paper, live (correctly, by design). External [A]: SR 11-7 (Fed/OCC 2011) "effective challenge" via *independent* validation; **SR 11-7 superseded by SR 26-2 April 2026** (retains independent-validation/effective-challenge expectations). Recommend: solo reaches RESEARCH→VALIDATION→SHADOW on self-validation-with-disclosure; PAPER/LIVE require a named external reviewer + a second authorized kill-switch resetter; do not weaken REQ-KILL-001. *[Lead note: governance-source relevance re-classified — SR 11-7/26-2 is bank supervisory guidance, INDUSTRY_BEST_PRACTICE / ANALOGICAL_GUIDANCE for this project, NOT legally binding; see PHASE_1_EVIDENCE_INTEGRITY_REVIEW.md and CX-11 reclassification.]*
- **A6-F10** Overnight completion SLA and funnel latency budget are TBD hard gates with no frozen wall-clock (REQ-GATE-001, REQ-NFR-001/002, REQ-CONV-OVN-014). MED · blocks paper, live. Recommend freeze concrete wall-clock + per-stage budget from observed `stage_durations`; on breach REQ-NFR-002 `BLOCK_ALL_NEW_ACTIONS` is the correct fail-safe.

## PROPORTIONATE-VS-OVER-SCOPED (summary)
KEEP (do not complexify): modular monolith (1 app/1 worker/1 DB/separate exec), 7-component kernel, research/runtime split, minimal runtime_manifest gate, capability-health, feature_snapshot contract, TBD-threshold governance metadata. SIMPLIFY/DEFER: event-sourcing hash-chain+outbox (→shadow), four bespoke registries (→MLflow+files), full Control Plane (→minimal manifest for R1A), reproducibility standard (→two-tier). DEFER (already): counterfactual digital twin (2B).

## VERDICT + TOP-5
Governance/reproducibility architecture is on balance sound and often commendably proportionate; problems are (a) one integrity breach in the evidence-tracking layer, (b) a few genuine internal contradictions, (c) three subsystems scoped as R0/R1A blockers that belong at shadow-era. Rejects any prior "terminal fail because the stack is fundamentally disproportionate" framing: the stack is *phased wrong and mis-evidenced*, not fundamentally wrong. No release beyond `shadow` for a genuine solo operator (F9). **Top-5:** F1 (reset coverage matrix) · F2 (two-tier reproducibility) · F9 (explicit solo governance cap) · F5+F6+F7 (rephase heavy stack) · F3+F10 (`environment_hash` + overnight SLA).

## SOURCE ENTRIES (agent-reported; re-graded in SOURCE_EVIDENCE_REGISTER.md)
Repo file-existence check (`ls`) [A]; in-repo spec/backlog contradictions [A]; SR 11-7 / SR 26-2 model-risk guidance (AnalystPrep, ModelOp, GARP secondaries of the Fed/OCC primary) [A, relevance re-classed to INDUSTRY_BEST_PRACTICE]; GPU/BLAS FP non-associativity (arXiv 2408.05148; RepDL arXiv 2510.09180; batch-invariant arXiv 2511.00025; mlf-core arXiv 2104.07651) [B]; feature-store/train-serve-skew (Confluent 2026; dev.to; Databricks) [B].
