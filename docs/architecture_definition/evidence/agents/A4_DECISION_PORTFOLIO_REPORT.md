# A4 — Decision-Policy-&-Portfolio-Architect — Round-1 Report (PRESERVED)

> **Provenance.** Final report preserved as delivered (cutoff 2026-07-21); substantive content faithfully retained, prose lightly condensed, nothing altered/added; complete raw = task-output JSONL. **SOURCE_REPORT_PRESERVED.** Context-isolated Claude subagent (same model family/provider).

---

**Scope:** decision layer + portfolio only (model internals→A3; microstructure/costs→A5). Repo docs-only; all "implementation" claims are design-state.

## FINDINGS
- **F-A4-01** `economic_edge_bps` return-basis unspecified; subtracting absolute cost from a sector-excess return is not executable P&L. HIGH · REQ-DEC-001, REQ-LAB-005/006, REQ-MOD-008/009 · blocks architecture_freeze, release_0_definition. `pretrade_expected_cost_bps` is an absolute drag; sector-excess is relative and, for a long-only book with no sector short/hedge, not harvestable. Recommend making it normative that `expected_return_bps` inside `economic_edge_bps` is the **absolute executable expected return** (`portfolio_return_target` basis); sector-excess belongs only to `alpha_score`/`rank_score` ordering, never the bps net-edge subtraction. Grade A (spec-internal).
- **F-A4-02** REQ-DEC-002 λ-weights have no fitting/estimation protocol; if backtest-fit they inherit the full PBO/multiple-testing burden (contrast REQ-STOP-002 which extends overfitting controls to stop/target). HIGH · blocks architecture_freeze, release_0_definition, final_holdout. Recommend declaring λ + score→bps map either `FROZEN_PRIOR` (hand-set, no data fitting — recommended MVP) or `FITTED` (then walk-forward + purged CV + PBO/deflated metric + explicit trial count mandatory). Grade A + B (Bailey & López de Prado DSR/PBO).
- **F-A4-03** No whole-decision (predicted-utility vs realized-net-return) calibration requirement (REQ-MOD-005 calibrates probabilities, not the utility output). MED-HIGH · blocks release_0_acceptance, paper. Recommend regressing realized net return on predicted `risk_adjusted_utility_bps`; require slope≈1, intercept≈0. Grade A (gap) + B (Niculescu-Mizil & Caruana).
- **F-A4-04** Horizon selection over {5,8,14} has no multiple-comparison control (REQ-HOR-001/002; joint distribution deferred R2). MED-HIGH · blocks release_0_acceptance, final_holdout. Recommend fixed-horizon default with challenger-promotion, or count all three in the deflated-metric trial budget. Grade A + B (Harvey-Liu-Zhu; DSR).
- **F-A4-05** `utility_by_state_hypothesis` (REQ-DOM-025) demands a regime-conditional calibrated forecast the model layer (REQ-MOD-002 single unconditional model) does not produce. MED · blocks none at MVP if scoped as overlay; would block release_1A if interpreted as a learned conditional forecast. Recommend implementing as a **scenario-stress overlay** (perturb μ/σ/tail by per-hypothesis regime deltas, weight by `state_probability`, report `worst_plausible_state_utility`), not a learned regime-conditional model; keep REQ-DOM-025 SHOULD/R2+. Grade A.
- **F-A4-06** Abstention/NO_ACTION first-class (good) but hurdle structurally defined, not numerically tied to cost + premium; all abstention thresholds TBD_BLOCKER. MED · blocks release_1A, paper. Recommend `required_hurdle_h ≥ pretrade_expected_cost_bps + risk/uncertainty premium`, versioned; default `NO_ACTION`; BUY requires `economic_edge_bps>0` AND `risk_adjusted_utility_bps>min_utility_hurdle` AND `permission_score>0` AND no hard blocker AND calibration-pass. Grade A.
- **F-A4-07** Effective independent sample for a decision-layer meta-model is orders of magnitude below raw row count (the crux for the contradictory hypothesis). HIGH (foundational) · informs architecture_freeze. Derivation: (a) 14-session overlapping labels → average uniqueness ≈ 1/14 (López de Prado); (b) cross-sectional dependence — N names load on few common factors, not N independent bets; (c) single-digit independent regime episodes/yr. Net effective sample governing decision-layer parameters ≈ **tens to low-hundreds of independent episodes**, not the six-figure raw count; purged+embargoed WF shrinks further. Grade A (REQ-VAL-001/002 present) + B (LdP average-uniqueness). *[Lead note: classified INFERENCE/derivation, not a measured value — see CLAIM_TO_SOURCE_TRACEABILITY.md CLM-04.]*
- **F-A4-08** Spec already forbids alpha→size directly (confirming, not a defect): REQ-MOD-011 score_ownership_matrix (Expected excess return, Sizing=No); REQ-MOD-006; REQ-RISK-002 sizing from risk budget ÷ stop distance. Grade A.
- **F-A4-09** Anti-anchoring: with 0 code + 8 TBD_BLOCKER thresholds + REQ-GATE-003, any PAPER_READY/LIVE_READY claim is false by construction — agreed regardless of prior audit. But the decision-layer **design** (penalized-utility kernel, deterministic permission, hard-blocker override, abstention-first, score-ownership matrix) is internally sound and literature-consistent. If prior verdict = "decision layer fatally flawed / rebuild," **DISAGREE** (defects are DEFINITION-gate fixes, not rewrites); if prior = "not ready; freeze thresholds and pin return basis," **AGREE**. Grade A.

## SIZING-PRECEDENCE CHAIN (ordered contract; a size is produced only at step 8 and can only shrink thereafter; no alpha/probability sets notional)
1 Calibrated forecast (REQ-MOD-003/005/007) — no · 2 Horizon selection (REQ-HOR, w/ MC control) — no · 3 Alpha/rank ordering (REQ-MOD-008/009) — no · 4 Permission gate (REQ-MOD-012/013) — gate · 5 Hard blockers (REQ-DEC-003) — force size 0 · 6 Economic edge (REQ-DEC-001, absolute basis) — gate · 7 Risk-adjusted utility (REQ-DEC-001/002) — gate · 8 Per-trade risk-budget size (REQ-RISK-002) — **first size** · 9 Hard caps (REQ-RISK-003) — shrink · 10 Portfolio marginal-utility allocation (REQ-PORT-002/003) — shrink/allocate · 11 Turnover/hysteresis (REQ-PORT-004) — can zero · 12 Execution feasibility (REQ-FILL-005/COST) — shrink/defer · 13 Human approval (REQ-DEC-005/006) — approve/reject. Blockers(5)/permission(4) precede sizing compute(8); steps 8→13 monotone non-increasing.

## DECISION-LAYER OPTION VERDICTS
Option 1 Deterministic rules + penalized utility → **MVP_READY/RELEASE_1A** (kill: frozen policy fails to beat naive/technical comparator after costs → no-edge release, do not escalate to learned layer to rescue). Option 2 Learned stacking/super-learner → **RESEARCH_ONLY (R4)** (fails F-A4-07 effective-sample; super-learner oracle needs K≤n^q, large n). Option 3 Mixture-of-experts → **RESEARCH_ONLY** (worse; gating adds params/calibration). Option 4 Bayesian decision model → **CANDIDATE (R2+)** (formalizes λ/hurdles as priors-with-uncertainty; natural `worst_plausible_state_utility`). Option 5 Constrained optimization → split: greedy marginal-utility allocation MVP_READY; full MV/utility optimizer RESEARCH_ONLY (DeMiguel: optimizers lose to 1/N OOS under estimation error).

## VERDICT + TOP-5
Contradictory hypothesis **SUPPORTED (Grade B)**: a learned super-model over the decision layer adds no reliable OOS value over a frozen deterministic penalized-utility policy at this effective sample size and cannot be safely trained (DeMiguel; super-learner oracle; Harvey-Liu-Zhu + DSR). MVP = **Option 1**, with: λ FROZEN_PRIOR (F-A4-02); `economic_edge_bps` on absolute-executable basis (F-A4-01); abstention-first (F-A4-06); no alpha/prob sets notional (F-A4-08). Option 4 (Bayesian) = R2+ successor. Options 2/3/5-optimizer = RESEARCH_ONLY. Decision layer is **spec-complete, unimplemented, unfrozen — not fatally flawed**. **Top-5:** F-A4-01, F-A4-02, F-A4-07, F-A4-04, F-A4-03.

## SOURCE ENTRIES (agent-reported; re-graded in SOURCE_EVIDENCE_REGISTER.md)
DeMiguel-Garlappi-Uppal 2009 *RFS* [B]; Harvey-Liu-Zhu 2016 *RFS* [B]; Bailey & López de Prado 2014 DSR + PBO [B]; super-learner oracle property (van der Laan/Polley summaries) [B/C]; Niculescu-Mizil & Caruana 2005 *ICML* [B]; López de Prado *AFML* (uniqueness/purge/embargo) [B/C]; internal spec REQ-DEC/MOD/RISK/PORT/HOR/DOM/VAL/GATE [A internal].
